
<%= render 'shared/navbar' %>


<div class="container">
  <h1>Tool Hire</h1>

  <div class="show-container">
    <div class="image">
      <% if @tool.photo.present? %>
        <%= cl_image_tag tool.photo.key, height: 300, width: 400, crop: :fill %>
      <% else %>
        <img height="300" width="400" src="https://hips.hearstapps.com/vader-prod.s3.amazonaws.com/1581705325-41z1obml8il-ac-1581705279.jpg">
      <% end %>
    </div>

    <div class="card-tool-info">
      <h2><%= @tool.name %></h2>
      <p><%= @tool.description %></p>
      <p><%= @tool.category %> </p>
    </div>

    <h2 class="card-tool-price">£££</h2>

    <!--
    <div class="card-tool-icons">
      <p>
        <button class="btn btn-primary" type="button" data-toggle="collapse" data-target="#icons" aria-expanded="false" aria-controls="collapseExample">
        More Info
        </button>
      </p>
      <div class="collapse" id="icons">
        <div class="card card-body">
          <i class="fas fa-battery-full"></i>
        </div>
      </div>
      -->

    </div>

    <button type="button" class="btn btn-primary"><%= link_to "Make a booking", new_tool_booking_path(@tool) %> </button>

    <div class="links">
      <%= link_to ('<i class="fas fa-arrow-circle-left"></i> Go back').html_safe, tools_path, class: "btn btn-outline" %>
      <!--
      <p><%= link_to edit_tool_path(@tool) do  %>
        <i class="fas fa-edit"></i>
      <% end %>
      <%= link_to ('<i class="fas fa-trash"></i>').html_safe, tool_path(@tool), method: :delete, data: { confirm: "Are you sure you want to delete " + @tool.name } %>
      </p>
      -->
    </div>
  </div>

  <div class="booking-form">
  
  
  </div>

</div>
